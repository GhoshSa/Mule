$kind: collection
description: |-
  REST API for the Mule order-management service (Spring Boot).
  Implements an order placement saga that reserves inventory atomically.
variables:
  baseUrl: http://localhost:8080
  orderId: "1"
